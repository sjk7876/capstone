1. re-annotate spencer 1 1-5 serves so they fit the file format (do all 1-20 of session 1)
2. change format_dataset to pull images from already processed stuff - can automate the frame extraction too
3. start pulling annotations from cvat w/o images
4. annotate ezra 3


5. restart current cvat instance and reexport project - check that all the serves are downloaded :P